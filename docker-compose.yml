version: '3.8'

services:
  google-plugin-store:
    image: youpingfang/google-plugin-store:latest
    ports:
      - "3005:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always